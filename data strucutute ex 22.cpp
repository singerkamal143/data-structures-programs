#include<Stdio.h>
#include<stdlib.h>

struct node
{
	int data;
	struct node*next;
}*top;

void push(int ele)
{
	struct node*n;
	n=(struct node *)malloc(sizeof(struct node));
	n->data=
	
}
